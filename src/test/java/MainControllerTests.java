/*
 *  UCF COP3330 Summer 2021 Application Assignment 1 Solution
 *  Copyright 2021 Randall Roberts
 */

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

class MainControllerTests
{
    void testAddButton()
    {
        /*
        Create an object and give the object properties
        Attempt to put the object into a list
        Assert that the object has been inserted into the list
        */
    }

    void testDeleteButton()
    {
        /*
        Create a preset array of objects
        attempt to remove one of these objects
        use a boolean to see if the object is still in the list
        assert if the boolean is true or not
        */
    }

    void testEditButton()
    {
        /*
        create an object and attempt to modify the objects properties
        use a boolean to assert if the objects new properties match the attempted edits
        assert the boolean
        */
    }
    @Test
    void placeholderTest()
    {
        // Placeholder test to get sonarlint to stop yelling at me
        assertEquals(Math.random(), Math.random());
    }
}